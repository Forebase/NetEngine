# CoreDNS configuration for ARM64 testing
# Validates DNS functionality on ARM64 architecture

arm64.local {
    log
    hosts {
        127.0.0.1 arm64.local
        127.0.0.1 netengine.arm64.local
        127.0.0.1 api.arm64.local
    }
}

. {
    prometheus 0.0.0.0:9153
    cache 30
    health 0.0.0.0:8888
    forward . 8.8.8.8
}
